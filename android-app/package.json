{
  "name": "life-list-android",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android wrapper for the Life List web app",
  "scripts": {
    "copy-web": "mkdir -p www/icons && cp ../index.html ../manifest.json ../sw.js www/ && cp ../icons/*.png www/icons/",
    "sync": "npm run copy-web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
